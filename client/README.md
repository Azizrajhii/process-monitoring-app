# Client

Frontend React + Vite + TypeScript.

## Dossiers

- `src/api` : appels HTTP vers le backend
- `src/components` : composants réutilisables
- `src/pages` : pages principales
- `src/types` : types TypeScript partagés côté frontend
